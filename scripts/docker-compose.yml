version: '2'
services:
  omar-avro-app:
    # networks:
    #   - docker_default
    image: omar-avro-app:latest
    volumes:
      - /dev/urandom:/dev/random
      # - /home:/home
      # - /Users:/Users
    ports:
      - "8080:8080"
networks:
  default:
    external:
      name: docker_default
